# Rendered output and verification artifacts: regenerated from the compositions,
# tens of megabytes each, never the source of truth.
renders/
snapshots/
.thumbnails/

node_modules/
.DS_Store

# Local secrets (ElevenLabs key). Never committed: this repo is shared on GitHub.
.env.local

# Studio caches: edit backups and waveform peaks, regenerated locally.
.hyperframes/backup/
.waveform-cache/
